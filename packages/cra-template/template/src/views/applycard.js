import React from 'react';
import { NavBar } from '../components';

const ApplyCard = () => (
  <div id="app" className="d-flex flex-column h-100">
    <NavBar />
    <h1>This is Apply Card page</h1>
  </div>
);

export default ApplyCard;

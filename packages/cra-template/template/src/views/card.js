import React from 'react';
import { NavBar, CardContent } from '../components';

const Card = () => (
  <div id="app" className="d-flex flex-column h-100">
    <NavBar />
    <CardContent />
  </div>
);

export default Card;

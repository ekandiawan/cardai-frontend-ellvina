import React, { Fragment } from 'react';

import { NavBar, Hero } from '../components';

const Home = () => (
  <Fragment>
    <NavBar />
    <Hero />
    <hr />
  </Fragment>
);

export default Home;
